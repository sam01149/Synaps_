import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Synapse AI | Enterprise Intelligence On Your Hardware",
  description: "Deploy autonomous AI agents securely behind your firewall. Synapse AI ensures 100% data sovereignty for Indonesian enterprises. No data leaves your building.",
  keywords: ["AI", "Enterprise", "Data Sovereignty", "Local AI", "Indonesia", "Secure AI"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-black`}
      >
        {children}
      </body>
    </html>
  );
}
